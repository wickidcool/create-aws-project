---
phase: 29-publishing-and-generated-project-template
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/create-aws-project-mcp/tsconfig.json
  - packages/create-aws-project-mcp/package.json
  - package.json
autonomous: false

must_haves:
  truths:
    - "npm pack --dry-run in packages/create-aws-project-mcp shows zero __tests__ files"
    - "npm run build from root succeeds for both packages"
    - "npm test from root passes all existing tests"
    - "packages/create-aws-project-mcp/package.json has description, keywords, repository, author, license fields"
    - "Root package.json version is 1.8.0"
  artifacts:
    - path: "packages/create-aws-project-mcp/tsconfig.json"
      provides: "Excludes test files from compilation output"
      contains: "src/__tests__"
    - path: "packages/create-aws-project-mcp/package.json"
      provides: "Publishable package with metadata and scripts"
      contains: "prepublishOnly"
    - path: "package.json"
      provides: "Root package at v1.8.0"
      contains: "1.8.0"
  key_links:
    - from: "packages/create-aws-project-mcp/tsconfig.json"
      to: "dist/"
      via: "exclude array prevents test compilation"
      pattern: "exclude.*__tests__"
---

<objective>
Prepare the MCP package for npm publishing by fixing the tsconfig to exclude test files from dist, adding npm metadata fields, and bumping the root package version to 1.8.0.

Purpose: The published tarball must contain only production code. Without the tsconfig fix, 21 compiled test files ship to npm. Package metadata makes the package discoverable and professional.
Output: A clean, publishable MCP package verified by npm pack --dry-run.
</objective>

<execution_context>
@/Users/alwick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alwick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/create-aws-project-mcp/package.json
@packages/create-aws-project-mcp/tsconfig.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix tsconfig exclude, add package metadata, bump root version</name>
  <files>
    packages/create-aws-project-mcp/tsconfig.json
    packages/create-aws-project-mcp/package.json
    package.json
  </files>
  <action>
    1. In `packages/create-aws-project-mcp/tsconfig.json`:
       - The current `exclude` array is `["node_modules", "dist"]`
       - Add `"src/__tests__/**/*"` to the exclude array so test files are not compiled into dist

    2. In `packages/create-aws-project-mcp/package.json`:
       - Update `description` to: `"MCP server for create-aws-project — exposes project scaffolding, AWS setup, and GitHub initialization as Claude/Cursor tools"`
       - Add `keywords`: `["mcp", "model-context-protocol", "aws", "claude", "cursor", "create-aws-project"]`
       - Add `repository`: `{ "type": "git", "url": "https://github.com/wickidcool/create-aws-project.git", "directory": "packages/create-aws-project-mcp" }`
       - Add `author`: `"wickidcool"`
       - Add `license`: `"ISC"`
       - Add `"clean": "rm -rf dist"` to scripts (prepublishOnly already exists)

    3. In root `package.json`:
       - Bump `version` from `"1.7.0"` to `"1.8.0"`

    4. Clean and rebuild:
       ```
       cd packages/create-aws-project-mcp && rm -rf dist && cd ../..
       npm run build
       ```

    5. Verify the tarball is clean:
       ```
       cd packages/create-aws-project-mcp && npm pack --dry-run
       ```
       Confirm NO `dist/__tests__/` entries appear.

    6. Run full test suite from root:
       ```
       npm test
       ```
       All tests must pass (currently 247).
  </action>
  <verify>
    Run from project root:
    - `npm run build` succeeds
    - `cd packages/create-aws-project-mcp && npm pack --dry-run 2>&1 | grep __tests__` returns empty (no test files in tarball)
    - `npm test` passes all tests
    - `grep '"version"' package.json` shows 1.8.0
    - `grep '"description"' packages/create-aws-project-mcp/package.json` shows non-empty description
  </verify>
  <done>
    MCP package tarball contains only production files (no __tests__), package.json has full npm metadata, root version is 1.8.0, all tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>MCP package is ready for npm publish. The tarball is clean (no test files), metadata is complete, version is 1.8.0.</what-built>
  <how-to-verify>
    1. Review the npm pack output (executor will have printed it)
    2. When ready, publish to npm:
       ```
       cd packages/create-aws-project-mcp
       npm whoami          # check if logged in
       npm login           # if needed
       npm publish --access public
       npm view create-aws-project-mcp   # verify it's live
       ```
    3. Optionally test fresh install: `npx -y create-aws-project-mcp` should start without error (Ctrl+C to stop)
  </how-to-verify>
  <resume-signal>Type "published" after publishing to npm, or "skip" to defer publishing</resume-signal>
</task>

</tasks>

<verification>
- `cd packages/create-aws-project-mcp && npm pack --dry-run 2>&1 | grep -c __tests__` returns 0
- `npm run build` from root succeeds
- `npm test` from root passes all tests
- `jq '.version' package.json` returns "1.8.0"
</verification>

<success_criteria>
MCP package is publishable: clean tarball, complete metadata, prepublishOnly builds automatically, root version bumped to 1.8.0.
</success_criteria>

<output>
After completion, create `.planning/phases/29-publishing-and-generated-project-template/29-01-SUMMARY.md`
</output>
